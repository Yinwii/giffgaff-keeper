# SMV Tool 数据管理与看板系统实现计划

当前需要在现有的项目或单独项目中实现 **SMV Tool** 模块，以管理和呈现从两份 Excel 提取的数据（跟踪清单与工序变更对比），支持人员权限控制、按人/机型 Tab 分类呈现、上传更新数据、带格式的导入导出等。

---

## 📌 架构选择对比 (Architecture Evaluation)

针对以何种方式开发此系统，进行如下对比分析：

| 维度 | 方案 A：在现有的 `WorkMangedb` 中增加侧边菜单 | 方案 B：作为一个单独的项目独立开发 |
| :--- | :--- | :--- |
| **开发工作量** | **极小 (推荐)**。可复用已有的 Flask+SQLAlchemy 基础骨架、Flask-Login 权限校验拦截、全局双主题机制及 `base.html` 侧边栏布局。 | **极大**。需要从头搭建用户认证、数据库管理、静态资源、主题切换等基础底座。 |
| **用户体验** | **优秀**。用户使用统一的登录入口、统一的字号缩放及侧边菜单，无缝在各个子工具间切换，数据资产不割裂。 | **一般**。用户需要记忆新的域名/端口，且存在两套独立的登录状态，使用体验不连贯。 |
| **数据与模型复用** | **极高**。原有系统已定义 `ProductModel`、`SMVData`、`CFCData`、`ECData` 等表结构，只需在其基础上进行适配和增强即可。 | **零**。需完全重建数据实体和关联逻辑。 |
| **部署维护成本** | **低**。只需运行已有的 Python/Flask 服务，且能复用已有的 OneDrive 后台自动文件同步与 Watcher 监视器机制。 | **高**。需要为新服务配置独立的后台运行进程、端口及同步脚本，占用系统额外内存。 |

**结论**：强烈建议采用 **方案 A（在现有的 `WorkMangedb - 复件(9)` 中以增加侧边菜单的方式实现）**。这不仅最利于系统的统一与维护，开发效率也将大幅提升。

---

## 🛡️ 需求梳理与呈现逻辑 (Requirements & Logic)

### 1. 合并去重后的规范表格字段 (Standardized Table Columns)
由于原 `Horizon` 和 `Merlin26` 两份文件字段多且不同（例如 Horizon 多达 27 列，Merlin26 有 21 列），我们需要整理一套标准的、支持后续灵活调整的数据库大宽表结构。
在文件名中的 `机型` 信息（如 Horizon, Merlin26）及 `上传日期` 将分别被提取并存入单独的物理列。

**合并规范表格列设计草案**：
*   **基础信息列**：
    *   `Project (机型/项目)`：如 Horizon, Merlin26（来自文件名/配置）
    *   `Sales Model (销售机型/MTM)`：如 PBAY0003US, PBB60000GB
    *   `Region (区域)`：如 USA, EMEA, LATAM
    *   `Carrier (运营商)`：如 RETAIL, VERIZON
    *   `Upload Date (更新日期)`：数据入库或上传文件的日期
*   **工序与状态监控列**（共用负责人/部门关系）：
    *   为了实现“按人员（责任人）进行数据分类与维护”：每一列都在系统中维护其对应的 **Default Owner (默认负责人)** 和 **Department (部门)**（继承自 Excel 表头第 2-3 行）。
    *   状态列包含：`CFC BOM`, `XCVR`, `XCVR in MOC`, `Label`, `SMV request`, `SVNkit#`, `BO2+Box opening`, `SMV Report`, `SMV Completed Date`, `SMV Approve`, `SA EC`, `SQA`, `Sales Model SA`, `SVNkit#2`, `Re-BO2`, `Re-BO2 completed date`, `Sales Model Re-SA` 等。

### 2. 工艺工序变更看板设计 (Sheet1 变更呈现看板)
对应 `MERLIN26 CFC sales model list.xlsx` 的 `Sheet1` 中展示的工序变更前后对比。
*   **看板核心作用**：直观展示特定销售机型在“变更前”与“变更后”工序的演进（例如：插卡、烧录、CQA、镭射的流动变化，新增 SIM卡绑定与插卡等）。
*   **UI/UX 呈现样式**：
    *   提供双轨对比的卡片式横向流程链（以颜色和连接线标示工序步骤）。
    *   显示工位动作注释（如“双工位，需要顶卡托”、“检查手机内是否有装SIM卡”），对于高风险项在视觉上进行红粗高亮警示。
    *   看板支持实时更新和状态调整，且操作人修改状态时，会自动进行权限拦截和记录。

### 3. 多维 Tab 过滤与分类呈现 (Tab Aggregation)
提供三个维度的顶级切换页签：
*   **人员 Tab (Personnel View)**：一键聚合显示所有属于当前选中负责人的数据条目（便于各自快速查找数据和维护新数据）。
*   **机型 Tab (Project View)**：按主项目（Horizon / Merlin26）展示对应的清单。
*   **全部 Tab (All View)**：以无缝滚动的现代化宽表格展示全局数据。

### 4. 权限与安全机制 (Access Control)
*   **游客/非登录状态**：只读模式，隐藏所有的“上传数据”、“删除”、“数据维护”以及“导出”等按钮。密码字段及敏感信息自动掩码为 `***`。
*   **普通用户**：可以查看各自负责的数据，支持一键锁定各自的名下数据并进行录入与更新。
*   **系统管理员**：享有最高权限，可上传 Excel 覆盖或增量更新，支持物理删除与批量分配 Owner，可管理工序负责人配置。

---

## 🗺️ 演示 Demo 呈现规划 (Demo Implementation)

为了最直观、免部署地供用户查看看板和分类效果，我们将首先在 `WorkMangedb - 复件(9)` 中建立一个 **独立的 Demo 路由和页面**，加载 Mock 的真实 Excel 汇总数据，实现：
1.  **现代化双主题界面**（亮色/暗色，月牙与太阳 SVG 一键切换）。
2.  **机型与人员 Tab 交互**：点击 Tab 直接无刷筛选表格内容。
3.  **Sheet1 变更工序对比可视化看板**：用漂亮的 CSS 流程节点展示工艺演变。
4.  **带有高亮格式的模拟导出**，并且在上传区提供上传提示框。

---

## 🙋‍♂️ 用户确认与开放问题 (Open Questions)

> [!IMPORTANT]
> **请确认以下设计细节以方便我们精准编写代码：**
> 1. **默认负责人机制**：Horizon 文件中有一行指定了每一列的负责人（如 `CFC BOM` -> `LiuQM`）。是否要在系统中将这一列负责人映射作为全局的“默认分配规则”？即当导入新数据时，该工序的 Owner 自动初始化为对应的值。
> 2. **工序对比看板的数据源**：目前只有 `MERLIN26` 里面有 `Sheet1` 工序对比，`Horizon` 文件中是否也包含或后续会补充类似的对比页？我们将如何支持该对比表的上传与新增？

---

## 📅 实现步骤规划 (Proposed Milestone)

1.  **[M1 - Demo 演示]** 创建 `templates/dashboard/smv_demo.html` 及路由，实现看板和 Tab 的交互原型，并在现有的 `templates/navigation.html` 中新增一个侧边菜单项“SMV Tool 演示面板”。
2.  **[M2 - 数据合并建模]** 更新 `models.py`，建立合并后的 SMVData 模型，支持“机型”、“上传日期”和 Dynamic Column 配置。
3.  **[M3 -导入导出服务]** 编写 `dashboard_logic.py`，实现多 sheet、多文件导入时的智能去重、格式比对及带有复杂填充颜色的 openpyxl 格式化导出。
4.  **[M4 - 权限加固与联调]** 将系统登录态和 `.env` 权限限制（如 `PRODUCT_PAGE_PASSWORD` 或管理员鉴权）注入新路由，进行全平台功能交付。
