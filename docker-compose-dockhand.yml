services:
  dockhand:
    image: fnsys/dockhand:latest
    container_name: dockhand
    restart: unless-stopped
    ports:
      - "43000:3000"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - /root/data/docker/dockhand/dockhand_data:/app/data

volumes:
  dockhand_data: